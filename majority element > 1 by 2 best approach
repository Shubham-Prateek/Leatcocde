int ele=nums[0];
        int sum=0;
        for(int i=0;i<nums.size();i++)
        {
            if(nums[i]==ele)
            {
                sum++;
            }
            else
            {
                sum--;
                if(sum==0)
                {
                    sum=1;
                    ele=nums[i];
                }
            }
        }
        return ele;

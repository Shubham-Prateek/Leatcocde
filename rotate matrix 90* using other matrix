int matrixb[matrix.size()][matrix.size()];
        int x=matrix.size()-1;//(matrix.size(),matrix.size());
        for(int i=0;i<matrix.size();i++)
        {
            for(int j=0;j<matrix.size();j++)
            {
                matrixb[j][i]=matrix[x][j];
            }
            x--;
        }
        for(int i=0;i<matrix.size();i++)
        {
            for(int j=0;j<matrix.size();j++)
            {
                matrix[i][j]=matrixb[i][j];
            }
        }
